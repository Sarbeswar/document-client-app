export const environment = {
  production: true,
  apiGatewayUrl: '/api',
  tokenRefreshSkewSeconds: 60,
  workflowPollingMs: 7000,
  notificationPollingMs: 20000,
  sessionWarningMs: 5 * 60 * 1000
};
